version: '3.8'

services:
  mysql-db:
    ports:
      - "${DB_PORT_EXTERNAL}:3306"
  app:
    ports:
      - "${APP_PORT_EXTERNAL}:${APP_PORT_INTERNAL}"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysql-db:3306/${DB_NAME}?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC